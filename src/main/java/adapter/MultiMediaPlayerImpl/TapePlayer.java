package adapter.MultiMediaPlayerImpl;

import adapter.AbstractPlayer;
import adapter.MultiMediaPlayer;

public class TapePlayer extends AbstractPlayer implements MultiMediaPlayer {
    public TapePlayer() {
        super("Tape");
    }
}
