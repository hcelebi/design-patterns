package adapter.MultiMediaPlayerImpl;

import adapter.AbstractPlayer;
import adapter.MultiMediaPlayer;

public class CDPlayer extends AbstractPlayer implements MultiMediaPlayer {
    public CDPlayer() {
        super("CD");
    }
}
